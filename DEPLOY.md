# Camfit AI Growth Editor - 배포 가이드 (비개발자용)

이 가이드는 완성된 코드를 무료로 인터넷에 배포(Publish)하여 실제 URL을 만드는 방법을 아주 쉽게 설명합니다.

## 1. 사전 준비 (계정 만들기)
1. **GitHub 가입**: [github.com](https://github.com/)에 회원가입을 하세요.
2. **Vercel 가입**: [vercel.com](https://vercel.com/signup)에서 'Continue with GitHub'를 눌러 가입하세요.

## 2. 코드 저장 (GitHub 업로드)
현재 컴퓨터에 있는 파일들을 GitHub에 올려야 합니다. (이 과정이 어려우면 개발자에게 이 폴더 압축파일을 전달하고 "GitHub 리포지토리에 올려주세요"라고 하시면 됩니다.)
만약 직접 하신다면:
1. GitHub 웹사이트에서 'New Repository' 클릭.
2. 이름 대충 짓고 'Create repository'.
3. 'Uploading an existing file' 링크 클릭 후, 이 프로젝트 폴더 안의 모든 파일을 드래그해서 업로드.

## 3. 실제 배포 (Vercel)
가장 쉬운 배포 방법입니다.

1. **Vercel 대시보드** 접속 -> 'Add New...' -> 'Project' 클릭.
2. 방금 올린 GitHub 저장소(Repository) 옆의 'Import' 버튼 클릭.
3. **Configure Project** 화면에서:
   ### 3. Vercel Blob 설정 (사진 첨부용)
   에어테이블에 사진을 자동으로 첨부하려면 Vercel 저장소 설정이 필요합니다.
   1. Vercel 프로젝트 대시보드에서 **Storage** 탭 클릭
   2. **Connect Database** -> **Blob** 선택
   3. **Create New** 버튼을 눌러 생성 (자동으로 환경변수가 추가됩니다)
   - **Environment Variables** (환경변수) 섹션을 펼칩니다.
   - 다음 두 가지 값을 입력해야 AI가 작동합니다.
     - **Key**: `GOOGLE_API_KEY` / **Value**: (Google Gemini API 키 입력)
     - **Key**: `AIRTABLE_API_KEY` / **Value**: (Airtable API 키 입력 - 선택사항)
   - 'Add' 버튼을 눌러 추가합니다.
4. **Deploy** 버튼 클릭.

## 4. 완료
- 약 1~2분 뒤 화면에 축하 폭죽이 터지며 `Visit` 버튼이 생깁니다.
- 그 버튼을 누르면 나오는 주소(예: `camfit-growth.vercel.app`)가 바로 완성된 웹사이트입니다.
- 이 주소를 팀원들에게 공유하세요!

---
**💡 API 키 발급 받는 곳**
- **Google Gemini**: [aistudio.google.com](https://aistudio.google.com/app/apikey) 에서 'Create API key' 클릭.
- **Airtable**: Airtable 계정 설정 -> Developer Hub -> Personal Access Token 생성.
